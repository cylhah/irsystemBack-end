<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cylwyc.demo.dao.UserDao">
    <select id="queryUserTypeById" resultType="com.cylwyc.demo.domain.Type">
        SELECT typeId,typeName,typeDescription
        FROM user,type,userandtype
        WHERE user.userId=#{userId}
        AND  user.userId=userandtype.userId
        AND type.typeId=userandtype.typeId
    </select>
    <insert id="insertType">
        INSERT INTO
        userAndType(userId,typeId)
        VALUES
        (#{userId},#{typeId})
    </insert>
    <delete id="deleteType">
        DELETE FROM
        userAndType
        WHERE
        userId=#{userId}
        AND typeId=#{typeId}
    </delete>
</mapper>